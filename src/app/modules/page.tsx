import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Modules · Emma',
};

export default function Modules() {
  return (
    <main className="lightpage" data-page="modules">
      <h1>Modules</h1>
    </main>
  );
}
